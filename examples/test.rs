/*use bevy::prelude::*;

fn main() {
    // App::new().add_system(some_system.system());
}

struct Player;
struct SpawnPlayer;
struct Test<T>(T);
// #[bevycheck::system]
// fn some_system(query: Query<Entity, Or<(Player, SpawnPlayer)>>) {}
*/

fn main() {}
